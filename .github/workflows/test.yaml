name: Tests

on: [ push ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: karabo-pipeline

jobs:
  Build_Dev_Image:
    id: build-dev-image
    outputs:
      imagetag: ${{ build-dev-image.outputs.imagetag }}
    uses: ./.github/workflows/build-dev-docker-image.yaml
    with:
      REGISTRY: ${{ env.REGISTRY }}
      IMAGE_NAME: ${{ env.IMAGE_NAME }}


  Test_Formatting:
    if: always()
    needs: Build_Dev_Image
    runs-on: ubuntu-latest
    container: ghcr.io/i4ds/karabo-pipeline:${{ needs.Build_Dev_Image.outputs.tag }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      - name: Install formatting-tools
        run: pip install -r requirements.txt
      - name: Check-flake8
        run: flake8 .
      - name: check-black
        run: black --check .
      - name: check-isort
        run: isort --check .

  Test_Karabo:
    if: always()
    needs: [ Test_Formatting, Build_Dev_Image ]
    runs-on: ubuntu-latest
    container: ghcr.io/i4ds/karabo-pipeline:${{ needs.Build_Dev_Image.outputs.tag }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      - name: Test Code
        shell: bash -l {0}
        run: |
          export IS_GITHUB_RUNNER=true
          export RUN_SLOW_TESTS=false
          python -m xmlrunner -o ./test-reports/junit
      - name: Publish Unit Test Results
        uses: EnricoMi/publish-unit-test-result-action@v1
        if: always()
        with:
          files: test-reports/**/*.xml

  Purge_Dev_Image:
    if: always()
    needs: [ Build_Dev_Image, Test_Formatting, Test_Karabo ]
    uses: ./.github/workflows/dev-workflow.yaml
    with:
      IMAGE_NAME: ${{ env.IMAGE_NAME }}
      IMAGE_TAG: ${{ needs.Build_Dev_Image.outputs.imagetag }}
